# Operations Control Panel SDD Progress

Plan: docs/superpowers/plans/2026-07-21-operations-control-panel.md
Base commit: 2ad0af17c0e32659e8137078ec75aaad7e01be01
Protected user change: scripts/get_youtube_token.py

- Task 1: complete (`2ad0af1..6fbd3c9`; 60 focused/legacy tests; reviewer approved)
- Task 2: complete by explicit user direction (`6fbd3c9..c430a69`; do not redispatch)
- Resume baseline at `c430a69`: frontend build passed; Python suite 118 passed / 1 known Task 8 fixture failure (`tests/integration/test_pipeline.py::TestAnalysisEngineIntegration::test_django_srt_pipeline`, expected 100 vs actual 200).
- Task 3: complete (`c430a69..624db5f`; 89 focused tests; Ruff/diff clean; reviewer approved). Minor tracked: 17 third-party `pysrt` deprecation warnings; two shared-fix regressions were mutation-verified rather than literal test-first chronology.
- Task 4: complete (`624db5f..273666a`; 55 focused/persistence/route tests; Ruff/diff clean; reviewer approved). Minor tracked for Task 7: four FastAPI `on_event` deprecation warnings; full-suite known Task 8 fixture failure remains.
- Task 5: complete (`273666a..7331fa8`; final affected suite 75 passed, heartbeat stress 20/20 root + 50/50 implementer, full suite 242 passed / 1 known Task 8 fixture failure, changed-file Ruff/diff clean; original reviewer approved with no Critical/Important findings). Implemented lease-fenced crash-recoverable artifact bundles, validated resumable generation, version-aware media/preview consumers, exact subtitle/audio provenance, cancellable media work, stale-owner publication fencing, and immediate-first heartbeat renewal. Non-blocking Minors tracked: graph preview relies on ordered `paths[-1]`, graph/composite hash the broad video config, and the secondary media probe observes cancellation at its bounded timeout.
- Task 6: complete (`7331fa8..7283199`; final focused 166 passed, unit + job submission 346 passed, full suite 379 passed / 1 known Task 8 fixture failure, changed-file Ruff/diff clean; controller reviewer spec-compliant and Ready with no Critical/Important findings). Implemented migration v3 and owner/expiry-fenced publishing attempts, crash ambiguity/reconciliation without blind retry, metadata single-generation, typed platform boundaries, strict remote IDs and native metrics, supplemental verified dimensions, and last-good statistics preservation. Migration v3 was an unavoidable schema-neutral-scope deviation required to distinguish live from abandoned uploads and conservatively recover true v2 active attempts.
- Task 7: complete (`7283199..6147fa8`; final focused API 72 passed, unit + API 413 passed, full suite 451 passed / 1 known Task 8 fixture failure, changed-file Ruff/diff clean; controller reviewer spec-compliant and Ready with no Critical/Important findings). Implemented fail-closed bearer auth, structured request-ID error envelopes, strict CORS and DTO boundaries, lifespan startup/recovery, operational reads/actions, bounded crash-recoverable uploads, durable publishing/stat seams, compatibility analytics with true aggregates, and scoped keyed/no-key idempotency auditing.
- Heartbeat follow-up: complete (`6147fa8..d67dfc3`; no production change). Captured a 141.5 ms scheduler suspension proving the 120 ms test observation equalled/exceeded its compressed lease; replaced wall-clock assumption with condition-based expiry advancement under a 600 ms lease. Final root stress 20/20 and full suite 451 passed / 1 known Task 8 fixture failure; independent review approved.
- Task 7: complete (`7283199..2d5fa69`; final focused 51 passed, unit + API 392 passed, full suite 430 passed / 1 known Task 8 fixture failure, changed-file Ruff/diff clean; independent medium review spec-compliant and Approved with no Critical/Important/Minor findings). Implemented authenticated strict operational DTO routes, generated request IDs and common envelopes, exact CORS/preflight handling, injected lifespan/dispatcher wiring, durable idempotent actions, crash-recoverable confined subtitle uploads, durable publishing controls, and sanitized compatibility reads.
- Task 7 controller remediation: complete (`2d5fa69..15d2f09`; final focused 67 passed, unit + API 408 passed, full suite 446 passed / 1 known Task 8 fixture failure, changed-file Ruff/diff clean; independent medium review spec-compliant and Approved with no Critical/Important/Minor findings). Closed IMDb/body validation, CORS policy envelopes and credentialed origin handling, scoped durable idempotency conflicts, strict compatibility/publishing DTOs, truthful unbounded aggregate counts, and authenticated JSON API roots.
- Task 7 final closure: complete (`15d2f09..d9cd284`; final focused 70 passed, unit + API 411 passed, full suite 449 passed / 1 known Task 8 fixture failure, changed-file Ruff/diff clean; independent medium re-review spec-compliant and Approved with no Critical/Important/Minor findings). Finalized keyed and no-key reconciliation scope across platform/outcome/remote identity, browser-visible request IDs on allowed-origin early failures, and strict bounded segment-info DTO projection.
- Task 7 no-key audit: complete (`d9cd284..6147fa8`; final focused 72 passed, unit + API 413 passed, full suite 451 passed / 1 known Task 8 fixture failure, changed-file Ruff/diff clean; independent medium re-review spec-compliant and Approved with no Critical/Important/Minor findings). Audited both opposite no-key reconciliation directions and remote mismatches for transactional durable rejection, accurate mismatch diagnostics, stable 409 envelopes, and no wake/release mutation.
- Task 8: complete (`d67dfc3..0efa221`; focused 41 passed; full suite 492 passed; required Ruff/diff clean; independent medium re-review approved with no Critical/Important/Minor findings). Shared durable CLI/preview paths, exact-run claims with heartbeat cleanup, truthful artifact validation/exits, safe env/provider defaults, and fixture remediation are implemented; protected token script remains unstaged.
- Task 9: complete (`0efa221..3f8c343`; frontend 65 passed; production build/diff clean; independent medium re-review approved with no Critical/Important/Minor findings). Added the secure operations API client/media seams, abortable race-safe polling, session-only app context, accessible toast/resource states, and deterministic frontend test foundation; protected token script remains unstaged.
- Task 10: complete (`3f8c343..fbe0fb3`; focused Task 10 + authorized ToastRegion regression 26 passed, full frontend 90 passed, production build/diff clean; independent medium re-review approved with no Critical/Important/Minor findings). Rebuilt the graphite/mint shell and operations queue with authoritative totals, grouped durable states, URL-owned race-safe filters/pagination, session auth gating, responsive accessible navigation, status/error boundaries, and idempotent inline submission. Authorized Task 9 deviation: the notification container is now a semantic labelled region, regression-covered. Explicit staged debt: JobDetail token/DTO integration belongs to Task 11; leaderboard/costs/alerts protected-token integration belongs to Task 12. Protected token script remains unchanged and unstaged.
- Task 11: complete (focused workspace + authorized polling-hook regression 35 passed, full frontend 109 passed, production build/diff clean; independent medium final re-review approved with no Critical/Important findings). Added the token-authenticated timeline-centric live job workspace, durable stage/attempt truth, abortable incremental events, shared mutation coordination through authoritative refresh, bounded/redacted diagnostics, subtitle operator controls, and explicit publishing/reconciliation surfaces. Authorized Task 9 deviation: `usePollingResource.refresh()` is now awaitable, regression-covered. Task 12/shared frontend polish owns two non-blocking Minors tracked in the Task 11 report: duplicate global/inline delegated-error announcements and an inert stale retry affordance that can look enabled during mutation; both are deferred because safety/visibility are correct and clean remediation crosses shared presentation boundaries. Protected token script remains unchanged and unstaged; Task 12 media/secondary components remain untouched.
- Task 12: complete (focused 18 passed, shared/review 42 passed, API 19 passed, full frontend 131 passed, build 70 modules, full Python 493 passed, Ruff/diff clean; independent medium final re-review approved with no Critical/Important/Minor findings). Implemented explicit-token identity-fenced blob media with backend FPS and complete URL cleanup, durable JobDetail preview integration, strict honest leaderboard/cost/revenue/alert routes, and both Task 11 shared-polish Minors. Authorized narrow deviations: backend FPS DTO/route, JobDetail media mount, shared error/retry presentation, and reviewer-requested authenticated `AlertBanner` shell mount. Initial review and re-review findings all received direct regressions and were closed. Protected token script remains unchanged and unstaged.
- Task 12 committed post-hoc by user direction (`60a2b95..4acd8e6`; verified: Ruff/diff clean, 131 frontend + 493 Python passed, build 70 modules) — Codex had left the work uncommitted when credits ran out.
- Task 13: complete (`4acd8e6..pending`; full Python 496 passed incl. new packaging scan, 131 frontend passed, build 70 modules, Ruff/diff clean). Made `.dockerignore` tracked and correct (excludes secrets/venvs/outputs/caches/node_modules; keeps `uv.lock`) with a `tests/unit/test_packaging.py` regression scan; declared Node 20+ (`.node-version`, package.json `engines`); rewrote CI into split backend (full pytest + ruff on src/api/tests) and frontend (npm ci + vitest + build) jobs; added a canonical `make verify` gate and corrected Make/setup ports (8001) and commands; built a multi-stage non-root Dockerfile (Node UI build → locked-deps Python runtime serving `api.main:app` on 8001 via PYTHONPATH, with `/api/health` HEALTHCHECK) and Compose volumes for `data`/`results`/`output`; and rewrote README plus authored `docs/operations-control-panel.md` and the `docs/codebase-review.md` review ledger. Protected token script remains unchanged and unstaged. (Committed `fbd045a`.)
- Task 14: complete (`fbd045a..pending`; new `tests/integration/test_operational_scenarios.py` 12 passed; full Python 508 passed, 131 frontend passed, build 70 modules, Ruff/diff clean). Added end-to-end scenario coverage for all twelve acceptance scenarios — generation success, transient discovery retry, metadata retry exhaustion, three subtitle rejections, manual selection/duplicate-idempotent resume, duplicate resume no-op, restart recovery, deterministic render failure, cancellation, publish transient exhaustion, ambiguous publish + reconciliation, and stats-failure last-good retention — driving the real `PipelineRunner` (with production retry policy), `SubtitleService`, and `PublishingService` behind in-memory fakes and temp artifacts. **No production changes were required**: every scenario passed against the existing implementation, so this is purely additive verification. Orchestration scenarios use a linear stage list (composite's child mechanics are covered by `test_generation_scenarios`/`test_pipeline_runner`). Independent review left to the user's `/code-review`. Protected token script remains unchanged and unstaged.
